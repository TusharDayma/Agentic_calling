# Not required for local dev — vite.config.ts already proxies /api to
# http://localhost:8000. Add variables here only if you deploy frontend and
# backend on different domains and need an absolute API base URL.
VITE_API_BASE_URL=http://localhost:8000/api
